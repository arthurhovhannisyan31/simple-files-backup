#[cfg(test)]
mod test {
  // test end to end
  // missing json file
  // valid json config, files are copied, logs are written
  #[test]
  fn test() {
    assert_eq!(1, 1);
  }
}

// cases
// set of files and directories, with symlinks
// cmd assert
